interface:
  display_name: "VisionFlow"
  short_description: "Repo-specific patterns and workflows for VisionFlow"
  default_prompt: "Use the VisionFlow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true